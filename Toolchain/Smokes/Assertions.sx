struct Probe {
    var count:int

    func check() bool {
        self.count += 1
        return self.count == 1
    }
}

func main() {
    var probe = Probe(count:0)
    assert(probe.check(), "probe must be checked once")
    print(probe.count)
}
