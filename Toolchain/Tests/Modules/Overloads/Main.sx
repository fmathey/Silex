import Library
use Library.measure

struct Holder {
    var value:Library.Value
}

func main() {
    let holder = Holder(value:Library.Value(count:3))
    print(measure())
    print(Library.measure(2))
    print(holder.value.count)
}
