struct Interval<T> {
    var minimum:T
    var maximum:T

    func contains(value:T) bool {
        return value >= self.minimum && value <= self.maximum
    }
}

func main() {
    let interval = Interval<str>(minimum:"a", maximum:"z")
    print(interval.contains("m"))
}
