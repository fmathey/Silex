struct Resource {
    let handle:int
    drop {}
}

func propagate() Result<Resource,str> {
    let result = Result<Resource,str>.success(Resource(handle:1))
    let value = try result
    return Result<Resource,str>.success(move value)
}

func main() {}
